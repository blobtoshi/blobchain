// Thin re-export — actual app body lives in BlobChainApp.tsx so it can be
// mounted from both the website and the desktop-app.
import BlobChainApp from "./BlobChainApp";
export default BlobChainApp;
